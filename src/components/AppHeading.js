import React from "react";
import classes from "./AppHeading.module.css"

const AppHeading = () => {
    return <div className={classes['app-heading-div']}>
        <h2>Flip a coin.</h2>
    </div>
}

export default AppHeading;